NODE_ENV=production
ADMIN_PORT=8090

# Required: generate a long random value, e.g.:
# openssl rand -hex 32
JWT_SECRET=<replace-with-long-random-secret>

# Demo seed account. Do not use personal contact information.
ADMIN_EMAIL=<demo-admin@example.invalid>
ADMIN_PASSWORD=<replace-with-strong-demo-password>

MONGO_INITDB_DATABASE=vuelos
